# Legacy append workflow

These scripts belong to the earlier workflow that appended missing canonical
proteins to an already-existing large CSV. They are retained for provenance,
but they are not called by `build_dataset.py` and should not be used for a new
from-scratch rebuild.
